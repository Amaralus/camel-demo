server:
  port: 9090

app:
  kafka:
    topic-in: "test-in"
    topic-out: "test-out"

camel:
  component:
    kafka:
      brokers: "localhost:9094"
      group-id: "camel-demo"
  springboot:
    main-run-controller: true